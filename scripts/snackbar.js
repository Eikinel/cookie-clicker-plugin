class Snackbar {
    constructor(title, message, timeout = 3000, image = null) {
        this.title = title;
        this.message = message;
        this.timeout = timeout;
        this.image = image;
    }
}